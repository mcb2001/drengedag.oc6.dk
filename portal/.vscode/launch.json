{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "runtimeExecutable": "yarn",
            "name": "yarn run start",
            "runtimeArgs": [
                "run",
                "start"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}